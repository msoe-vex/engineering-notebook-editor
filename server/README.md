# Collaboration Service

This service runs a Hocuspocus + Yjs WebSocket server for real-time collaborative editing.

## Local run

```bash
npm install
npm run dev
```

Environment variables:

- `PORT` (default `1234`)
- `COLLAB_AUTH_DISABLED` (set to `true` for local unauthenticated development)
